---
name: push
description: Add, Commit and Push the local branch to a remote repository.
---

Stage all changes, create a commit with a clear message describing the work, and push the current branch to its remote. If the branch has no upstream, set it with `git push -u origin <branch>`.
