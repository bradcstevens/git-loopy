Right now when the user runs the git loopy command after installing or setting up git loopy on their machine, everything gets set up depending on what the user chooses, either a local project set up or globally. But it doesn't seem to be setting up every skill that we have in this project as a part of that initialization. When running Git Loopy for the first time and initializing it either in a product or globally, it seems like just the setup agent skills skill is automatically set up under the .copilot/front-end/skills folder. We need to make sure that all the skills in this repository for Git Loopy are automatically added when a user is initializing Git Loopy in their project or globally.