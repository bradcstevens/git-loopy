We need to add individual context window control to sessions that are created to work on issues/tickets by git-loopy. Not all input-output tokens are consumed equally as various tasks and reasoning occurs within a given context window session. This presents a specific challenge when we want to set a given session being worked on by our loop. We should try to target a session that is around 100,000 tokens. In a lot of cases, me as the human may be driving a session, oftentimes with a skill that I'm triggering. For example, the `/grill-me` and `grill-with-docs` and `grilling` skills ask multiple questions, sometimes many before concluding with a grill session. As the context window fills on whatever model that I'm using, I'm watching the context window fill up. Most of the time, when I get to the end of the questions from these skills, I'm usually around 100,000 to 150,000 tokens in context consumption. As soon as I see that the context window has gotten to about 100,000 tokens, I start to consider whether or not I should do a handoff and start a new session to continue that grilling session. I'll typically let one of these sessions continue up to about 150,000 tokens if I feel like I'm about to conclude the grill session. Only in rare cases would I go up to about 200,000 tokens. In an effort to stay around this 100,000 token context window session limit, I primarily rely on a skill called handoff, which will create a handoff document that I can pass in to a new session. For a new session to pick up and continue with. I prefer to use this skill because it gives me more direct control over how compaction and information from a given session carries over into the next session. I'm well aware that most agent harnesses like Claudcode, GitHub Copilot, CLI, Codex, and so on perform automatic compaction when reaching model context window limits. The problem that I have with this is that I, as the user driving these workflows, don't have control over how a plan is created or a handoff compaction is created by whatever agent harness I'm using. We need to build in some kind of control mechanism to ensure that when there are multiple issues, PRDs, etc., that our loop scripts pick up, that each session is created and only utilized up to somewhere around that hundred thousand, hundred fifty thousand token context window, and once that context window has been filled to around that range, we need to utilize something like the handoff skill to generate a handoff document and then to start a new session. While continuing to work on an issue until that issue is closed and completed, an issue may have one or more handoff docks generated and may sometimes mean creating multiple sessions until that issue is completed. As part of our context control, we need to ensure that when an issue is being worked on, that whenever a new session is created, either a new session or a session that's being resumed with a handoff document is input. Input for that specific issue, that only the relevant skills are loaded into that session for that issue to be completed. In other words, we need to ensure that there's no extra bloat for every session that's created and being worked on by our loop. This may not apply to the current product structure, so dismiss this specific part of our features to add if we're already doing a good job with the app framework for Git Loopy to ensure our context windows for each session is as token context window efficient as possible. A challenge I see is on my local MacBook, I'll open up the Copilot CLI. And as time goes on, I'll find and add many skills or MCP servers as well as whatever the harness has that might otherwise consume tokens underneath the hood when I use the CLI. Sometimes means starting a brand new session with a model, automatically starts me off at about 10% of that model's token context window already being consumed even before I started working on any tasks. With that new session, it's that exact problem that I want to ensure that GitLoopy doesn't succumb to and that only the most relevant skills, MCP, etc., are injected into a new session's context window every time a new session is created.