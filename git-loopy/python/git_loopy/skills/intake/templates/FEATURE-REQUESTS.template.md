# Feature Requests

> Generated by `/intake` on `<TIMESTAMP>`.
> Intended next step: `/grill-me` (greenfield) or `/grill-with-docs`.
> Scope: raw + lightly-refined feature intake only — **not** a PRD, spec, or implementation plan.
> Supporting context (terminal output, screenshots, logs) lives in `./context/`.

## Project context

`<ONE_OR_TWO_LINES: what this project is, and the area / command / surface these requests concern>`

## Requests

> Each request is independent and self-contained so it can be grilled on its own.
> The verbatim **Original wording** is preserved on purpose — the grill skills interrogate
> the user's actual phrasing, not just the cleaned-up version.

### 1. `<SHORT_IMPERATIVE_TITLE>`

**Original wording:**
> `<VERBATIM_USER_TEXT_FOR_THIS_REQUEST>`

**Request:**
`<REFINED_SELF_CONTAINED_DESCRIPTION_OF_THE_CHANGE>`

**Why:** `<USER_STATED_OR_CLEARLY_IMPLIED_MOTIVATION — omit this line if none was given>`

**Open questions for grilling:**
- `<AMBIGUITY_OR_DECISION_INTAKE_DELIBERATELY_LEFT_UNRESOLVED — omit section if none>`

**Context:** `<./context/FILE — why it's relevant, or "none">`

### 2. `<SHORT_IMPERATIVE_TITLE>`

**Original wording:**
> `<VERBATIM_USER_TEXT_FOR_THIS_REQUEST>`

**Request:**
`<REFINED_SELF_CONTAINED_DESCRIPTION_OF_THE_CHANGE>`

**Context:** `<./context/FILE — why it's relevant, or "none">`

## Added context

> Global context that applies across requests (request-specific context is linked inline above).

- `context/<FILE>` — `<ONE_LINE_DESCRIPTION>`
